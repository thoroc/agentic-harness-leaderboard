---
import type { SitePayload, HistoryPayload, HistorySnapshot } from "../data/types";
import { t } from "../i18n";

interface Props {
  payload: SitePayload;
  history: HistoryPayload;
  translations: Record<string, string>;
  lang: string;
}

const { payload, history, translations, lang } = Astro.props as Props;

function snapshotValue(snapshot: HistorySnapshot, id: string): number | null {
  return snapshot?.agents?.find((a) => a.id === id)?.stars ?? null;
}

function deltaFor(stars: number | null, id: string, days: number): number {
  const snapshots = history?.snapshots ?? [];
  if (!snapshots.length || typeof stars !== "number") return 0;
  const latestTime = Date.parse(payload.generatedAt);
  const target = latestTime - days * 24 * 60 * 60 * 1000;
  const ordered = snapshots.slice().sort((a, b) => Date.parse(a.generatedAt) - Date.parse(b.generatedAt));
  const baseline = [...ordered].reverse().find((item) => Date.parse(item.generatedAt) <= target) ?? ordered[0];
  const previous = snapshotValue(baseline, id);
  return typeof previous === "number" ? stars - previous : 0;
}

const agents = payload.agents.slice().sort((a, b) => (b.stars ?? -1) - (a.stars ?? -1) || a.name.localeCompare(b.name));

function shortDate(value: string | null): string {
  return value ? String(value).slice(0, 10) : t(translations, "notAvailable");
}

function kindLabel(kind: string): string {
  return kind === "open" ? t(translations, "kindOpen") : kind === "closed" ? t(translations, "kindClosed") : t(translations, "kindUnknown");
}

function searchText(agent: (typeof agents)[number]): string {
  return [agent.name, agent.repo, agent.vendor, ...agent.sources].filter(Boolean).join(" ").toLowerCase();
}
---

<section class="panel board-panel">
  <div class="panel-head board-head">
    <div>
      <p class="eyebrow">{t(translations, "leaderboard")}</p>
      <h2>{t(translations, "agentLeaderboard")}</h2>
    </div>
    <div class="toolbar">
      <div class="segmented" id="kind-tabs">
        <button type="button" class="active" data-kind="all">{t(translations, "filterAll")}</button>
        <button type="button" data-kind="open">{t(translations, "filterOpen")}</button>
        <button type="button" data-kind="closed">{t(translations, "filterClosed")}</button>
        <button type="button" data-kind="unknown">{t(translations, "filterUnknown")}</button>
      </div>
      <input id="search" type="search" placeholder={t(translations, "searchPlaceholder")} />
    </div>
  </div>
  <div class="table-wrap">
    <table>
      <thead>
        <tr>
          <th>{t(translations, "colRank")}</th>
          <th>{t(translations, "colAgent")}</th>
          <th>{t(translations, "colKind")}</th>
          <th>{t(translations, "colStars")}</th>
          <th>{t(translations, "col30D")}</th>
          <th>{t(translations, "colForks")}</th>
          <th>{t(translations, "colLastPush")}</th>
          <th>{t(translations, "colSource")}</th>
        </tr>
      </thead>
      <tbody id="leaderboard">
        {agents.map((agent, i) => (
          <tr data-kind={agent.kind} data-search={searchText(agent)}>
            <td>{i + 1}</td>
            <td class="agent-cell">
              <a href={agent.url ?? "#"}><strong>{agent.name}</strong></a>
              <span>{agent.repo ?? agent.vendor ?? t(translations, "notAvailable")}</span>
            </td>
            <td><span class="badge">{kindLabel(agent.kind)}</span></td>
            <td>{typeof agent.stars === "number" ? agent.stars.toLocaleString(lang) : t(translations, "notAvailable")}</td>
            <td>{deltaFor(agent.stars, agent.id, 30).toLocaleString(lang)}</td>
            <td>{typeof agent.forks === "number" ? agent.forks.toLocaleString(lang) : t(translations, "notAvailable")}</td>
            <td>{shortDate(agent.pushedAt)}</td>
            <td>{agent.sources?.join(", ") ?? ""}</td>
          </tr>
        ))}
      </tbody>
    </table>
  </div>
</section>

<script>
  // @ts-nocheck
  const tabsEl = document.getElementById("kind-tabs");
  const searchEl = document.getElementById("search");
  const tbody = document.getElementById("leaderboard");

  let currentKind = "all";
  let currentQuery = "";

  function filterRows() {
    if (!tbody) return;
    const q = currentQuery.trim().toLowerCase();
    for (const row of tbody.children) {
      const kind = row.getAttribute("data-kind");
      const search = row.getAttribute("data-search") ?? "";
      const kindMatch = currentKind === "all" || kind === currentKind;
      const queryMatch = !q || search.includes(q);
      row.style.display = kindMatch && queryMatch ? "" : "none";
    }
  }

  if (tabsEl) {
    tabsEl.addEventListener("click", (e) => {
      const button = e.target.closest("button[data-kind]");
      if (!button) return;
      currentKind = button.dataset.kind;
      tabsEl.querySelectorAll("button").forEach((b) => b.classList.toggle("active", b === button));
      filterRows();
    });
  }

  if (searchEl) {
    searchEl.addEventListener("input", () => {
      currentQuery = searchEl.value;
      filterRows();
    });
  }
</script>
